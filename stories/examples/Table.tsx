import React from 'react';
import type { SampleStory } from './util';
import { Table } from '../../src';

export const StandardTable: SampleStory = (args) => {
    return <Table {...args} />;
};

export const TableWithPagination: SampleStory = (args) => {
    return <Table {...args} />;
};

export const CustomizedTable: SampleStory = (args) => {
    return <Table {...args} />;
};
